# use a maintained OpenJDK runtime image
FROM eclipse-temurin:8-jre
# use shaded jar produced by maven-shade-plugin
ARG JAR_FILE=target/*.jar
# Docker 已停用。
# 请使用项目根目录的 start-local.bat 进行本地启动（JRE + MySQL 便携版）。
