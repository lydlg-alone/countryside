version: '3.8'

# Docker 已停用。
# 请使用项目根目录的 start-local.bat 进行本地启动（JRE + MySQL 便携版）。
    volumes:
      - ./mock-server:/usr/src/app:ro
    command: ["npm","start"]
    ports:
      - "8082:8082"
    depends_on:
      - app

volumes:
  mysql_data:
  redis_data:
